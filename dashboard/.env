# Dashboard environment configuration
# API endpoint for admin functionality

# Use the Static Web App's built-in API (default)
# VITE_API_BASE is unset, so it defaults to /api which routes to dashboard/api/